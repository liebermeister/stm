function p = elasticity_sampling_make_prior_pars(N,W,ext_ind,constraints,options);

% p = elasticity_sampling_make_prior_pars(N,W,ext_ind,constraints,options);

p.N           = N           ; 
p.W           = W           ;
p.ext_ind     = ext_ind     ;
p.constraints = constraints ;
p.options     = options     ;