function p = elasticity_sampling_make_likelihood_pars(network,c_init,data);

% p = elasticity_sampling_make_likelihood_pars(network,c_init,data);

p.data        = data; 
p.c_init      = c_init; 
p.network     = network;
